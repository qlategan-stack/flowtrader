# API keys — NEVER commit these
.env
*.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
*.egg
venv/
env/
.venv/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs and journals (kept locally, uploaded as GitHub artifacts)
journal/bot.log
journal/bot.log.*
journal/last_api_alert.json
# trades.jsonl is the canonical trade log. The dashboard sync workflow
# (push_journal.py) is the authoritative uploader; the bot's local copy is a
# rolling working set and must not be committed from the bot repo.
journal/trades.jsonl
journal/trades.jsonl.bak.*
journal/already_held_cooldown.json
*.log


# OS
.DS_Store
Thumbs.db

# Test outputs
*.tmp
test_output/

# Git worktrees
.worktrees/
